responses:
    200:
      description: The current data version
      schema:
          id: version
          type: object
          properties:
           version:
             type: object
             properties:
              version:
                  type: integer
                  description: Current data version number