[mysqld]
# Allow stored functions to be created without SUPER privilege when binary logging is enabled
# Required for puzzleboss schema initialization
# In production AWS RDS, set this via the Parameter Group
log_bin_trust_function_creators = 1
